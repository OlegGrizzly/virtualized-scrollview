using UnityEngine.UI;

namespace OlegGrizzly.VirtualizedScrollview.Abstractions
{
    public interface IViewAdapter
    {
        /*void Initialize(ScrollRect scrollRect, IVirtualDataSource dataSource, IVirtualCellFactory cellFactory, float itemHeight, float spacing, int preloadExtra = 2);

        void ScrollToIndex(int index, bool alignToTop = true);
        
        void ScrollToStart();
        
        void ScrollToEnd();
        
        void Refresh();
        
        void RebuildLayout();*/
    }
}